name: build-readme
on: 
    push:
    pull_request:
    schedule:
      - cron: '16 0/6 * * *'
jobs:
  start-update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v2
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8
